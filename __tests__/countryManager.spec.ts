import { fetchAllCountryOverviews } from "../src/api/requests";

// const countries = await fetchAllCountryOverviews();

// describe("Country Manager", () => {
//   it("should do some test case", ()  => {
//     expect(x).toBeInstanceOf(x);
//   });
// })
